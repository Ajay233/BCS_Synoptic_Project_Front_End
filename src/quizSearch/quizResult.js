import React from 'react'

const QuizResult = (props) => {

  const renderResult = () => {
    
  }

  return(
    <React.Fragment>
      {renderResult()}
    </React.Fragment>
  );

}

export default QuizResult;
